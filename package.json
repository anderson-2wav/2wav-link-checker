{
  "name": "2wav-link-checker",
  "version": "1.0.0",
  "description": "",
  "main": "src/cli.js",
  "bin": {
    "link-checker": "src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "test:wildlife": "node src/cli.js https://wildlifeillinois.org/sitemap.xml -o ./wildlife-illinois-link-report -f both -v --domain-concurrency 1 --domain-delay 1000",
    "publish:github": "git checkout public && git checkout main -- src/ package.json package-lock.json README.md .gitignore && git add -A && git diff --cached --quiet || git commit -m 'Sync from main' && git push github public:main && git checkout main"
  },
  "repository": {
    "type": "git",
    "url": "git@git.2wav.com:2wav/2wav-link-checker.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^14.0.3",
    "fast-xml-parser": "^5.5.9",
    "p-limit": "^7.3.0",
    "playwright": "^1.58.2"
  }
}
